name: dart_jsona
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: artemgalas <email@example.com>

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  json_annotation: ^1.0.0

dev_dependencies:
  build_runner: ^0.10.0
  test: ^1.0.0
  json_serializable: ^1.4.0
